import { colors } from "./colors";

const defaultStyles = {
  authLinkText: {
    marginTop: 10,
    color: colors.whiteText,
    textAlign: "center",
    fontSize: 18,
  },
  authLinkText2: {
    fontWeight: "bold",
  },
};

export default defaultStyles;
